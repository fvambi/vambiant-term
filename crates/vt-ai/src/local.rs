//! Local runtimes: Ollama native API (`/api/generate` with `suffix` → FIM),
//! `llama-server` (`/infill`, and its Anthropic-compatible `/v1/messages`),
//! MLX, LM Studio. Install commands are surfaced by `vterm ai doctor`
//! rather than assumed present.
