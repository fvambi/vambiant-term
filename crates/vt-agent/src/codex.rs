//! Codex adapter (docs/03 §5): `codex app-server` JSON-RPC client
//! (`thread/*`, `turn/*`, approval requests), hooks via an owned profile,
//! `codex exec --json`. Fixtures in `tests/fixtures/codex/`.
